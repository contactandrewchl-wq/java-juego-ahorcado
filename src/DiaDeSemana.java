/*
    Ejercicio de determinar el día de la semana:

Escribe un programa que solicite al usuario ingresar un número del 1 al 7 que represente
un día de la semana. Utilizando la estructura condicional if-else.
Muestra el nombre del día correspondiente (por ejemplo, 1 para "Lunes", 2 para "Martes", etc)

 */

import java.util.Scanner;

/* public class DiaDeSemana {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        String[] dias = {"Lunes", "Martes",
                "Miércoles", "Jueves", "Viernes",
                "Sábado", "Domingo"};

        System.out.print("Ingresa un número del 1 al 7 : ");
        int numeroDia = scanner.nextInt();

        if (numeroDia >= 1 && numeroDia <= 7) {
            int i = numeroDia - 1;
            System.out.println("El número que ingresaste corresponde al día " + dias[i]);
        } else {
            System.out.println("El número que ingresaste no corresponde");
        }

        scanner.close();
    }
}

 */